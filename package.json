{
  "name": "portfolio-gifino",
  "private": true,
  "version": "0.0.0",
  "packageManager": "npm@10.9.2",
  "engines": {
    "node": ">=20.19.0"
  },
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "dev:web": "turbo run dev --filter=web",
    "dev:studio": "turbo run dev --filter=studio"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
